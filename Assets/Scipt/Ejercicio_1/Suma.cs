using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Suma : MonoBehaviour {
    
	// Use this for initialization
	void Start () {
        //Imprime la suma de el texto concatenada con la suma de los dos enteros en el estar (una sola ves)
        print("2 + 5 = "+ (2 + 5));
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
